MC_SERVER_HOST=play.example.net
MC_SERVER_PORT=25565
# Microsoft account email that owns the Minecraft account (device-code login authenticates it)
MC_ACCOUNT_EMAIL=you@example.com
# Optional: pin the protocol version if auto-detection fails, e.g. "1.20.4"
MC_SERVER_VERSION=
WEB_PORT=3000
