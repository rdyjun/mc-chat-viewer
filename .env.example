WEB_PORT=3000

# Azure app registration (Entra ID) for "Sign in with Microsoft" — see README for setup steps.
MS_CLIENT_ID=
MS_CLIENT_SECRET=
MS_REDIRECT_URI=http://localhost:3000/api/account/callback
