services:
  app:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${WEB_PORT:-3000}:${WEB_PORT:-3000}"
    volumes:
      - ./nmp-cache:/app/nmp-cache
      - ./data:/app/data
